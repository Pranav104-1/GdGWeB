# GDG Website Backend Environment Configuration

# ==================== SERVER CONFIGURATION ====================
PORT=4565
NODE_ENV=development

# Frontend URL for CORS (update based on where frontend is hosted)
FRONTEND_URL=http://localhost:5000

# ==================== DATABASE CONFIGURATION ====================
# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster-name.mongodb.net/database-name?appName=AppName
# Replace the values below with your actual MongoDB credentials
MONGODB_URI=mongodb+srv://pranavnere647_db_user:PranavNere@gdgweb.ycog6lf.mongodb.net/gdg_website?appName=GDGWEB

# ==================== JWT CONFIGURATION ====================
# Generate strong random secrets for production!
# You can use: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_key_change_this_in_production
REFRESH_TOKEN_SECRET=your_refresh_token_secret_change_this_in_production

# ==================== EMAIL CONFIGURATION (Gmail) ====================
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and "Windows Computer"
# 4. Copy the 16-character password below

EMAIL_USER=your_gmail@gmail.com
EMAIL_PASSWORD=16_character_app_password_here

# ==================== CORS CONFIGURATION ====================
# Allowed origins for frontend requests
CORS_ORIGIN=http://localhost:5000

# ==================== TOKEN EXPIRY ====================
ACCESS_TOKEN_EXPIRY=24h
REFRESH_TOKEN_EXPIRY=7d

# ==================== PRODUCTION SETTINGS ====================
# When deploying to production, set:
# NODE_ENV=production
# FRONTEND_URL=https://your-frontend-domain.com
# And generate new, strong secrets for JWT
